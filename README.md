# Compiler Error Message Rewriting

## Setup

```bash
git clone https://github.com/charan-mv/Compiler-Error-Message-Rewriting.git
cd Compiler-Error-Message-Rewriting
pip install -r requirements.txt
```

## Run

```bash
python main.py
```

OR (if using Streamlit):

```bash
streamlit run app.py
```
